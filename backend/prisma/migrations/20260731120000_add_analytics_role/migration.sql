-- Роль «Аналитика»: read-only доступ.
-- Основное назначение — учётка, под которой коллектор/аналитический сервис
-- ходит в /api/v1/collector. Пользователь с этой ролью может также залогиниться
-- в UI, но на всех мутирующих endpoint'ах получит 403 (guards уже так настроены).
ALTER TYPE "Role" ADD VALUE IF NOT EXISTS 'analytics';
